<%= simple_form_for @submission do |f| %>
  <%= f.input :problem, collection: Contest.instance.problems %>
  <%= f.input :attachment, as: :file %>
  <% @submission.attachment.errors.full_messages.each do |message| %>
    <%= message %><br>
  <% end if @submission.attachment %>
  <%= f.submit 'Submit' %>
<% end %>
